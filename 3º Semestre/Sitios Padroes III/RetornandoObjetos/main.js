function calculaRetangulo(b, h) {
    // var valor_area = (b * h);
    // var valor_perimetro = (b + h) * 2;
    // return {
    //     area: valor_area,
    //     perimetro: valor_perimetro
    // };

    let area = (b * h);
    let perimetro = (b + h) * 2;
    return [area, perimetro];
};